// Single source where all LLVM Cish metadata nodes are declared. There are
// separate macros in LLVMCishMetadata.h and LLVMCishMetadata.cpp where
// the routines to handle the metadata are declared and defined

CISH_INST_METADATA(Ignore)
CISH_FUNC_METADATA(Ignore)
CISH_GLOBAL_METADATA(Ignore)